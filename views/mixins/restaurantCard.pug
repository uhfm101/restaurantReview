mixin restaurantCard(restaurant)
    a.card(href=`/restaurant/${restaurant.id}`)
        div
            div.card
                div.card-header
                    div.backdrop
                    img.card-header-image(src=restaurant.image)
                div.card-body
                    div.card-details
                        strong.emphasize #{restaurant.name} Owned by #{restaurant.owner.first_name}